---
description: Force DiffLore to learn from this session now
argument-hint: [optional note to capture]
allowed-tools: Bash(difflore:*)
---

Run DiffLore learning extraction now. If a note is provided after the command,
pass it through exactly so it can be considered alongside the recent session.

!`difflore learn --note "$ARGUMENTS"`
