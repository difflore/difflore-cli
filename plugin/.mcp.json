{
  "mcpServers": {
    "difflore": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/scripts/difflore-mcp.js"],
      "note": "Starts `difflore mcp-server` through the plugin runtime locator. Set DIFFLORE_BINARY to an absolute path for explicit local debugging."
    }
  }
}
